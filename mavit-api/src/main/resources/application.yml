server:
  port: 5000
spring:
  profiles:
    active: @activatedProperties@
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
mavit:
  app:
    jwtExpirationInMs: 86400000